# 2026-05-23

First day on the repo. Goal: get oriented, get something running, and lay
groundwork for adding Anthropic support so we can A/B compare against Gemini.

## Aims
- [x] Fetch `reilly_image_model_debug` and branch off as `vmsr`
- [x] Get `orientation_tuning` running end-to-end locally (Mac)
- [x] Verify Gemini API + JAX + scoring loop all work
- [x] Write `overview.md` so I can navigate the repo
- [x] Set up working conventions (`CLAUDE.md`, `journal/`)
- [x] Add Anthropic provider support (so model_llm can be Claude or Gemini)
- [x] Write a standalone test that pings each LLM API independently

## Done

### Branch setup
- `git fetch origin reilly_image_model_debug` → `git checkout -b vmsr FETCH_HEAD` → pushed to origin.

### Environment fixes (`requirements.txt`)
- Made `jax[cuda12]` Linux-only via `sys_platform` marker (was breaking macOS install).
- Unpinned `anthropic` and `google-genai`. The hard pins (`==0.50.0`, `==1.51.0`) were forcing pip to backtrack `pydantic-ai` down to 0.2.9, which doesn't have the `pydantic_ai.capabilities` module the code imports. Neither lib is imported directly in source — they're transitive through pydantic-ai — so unpinning is safe.
- Pinned `pydantic-ai>=1.0`.

### Smoke run for `orientation_tuning`
- Real Stringer data lives at `/home/dabin/...` on Reilly's machine. Generated synthetic placeholder via `data/_generate_dummy.py` → `data/gratings_drifting_GT1_2019_04_12_1.npy`. 200 cells × 600 trials with von-Mises orientation tuning so it survives the activity/concentration filters in `load_data.py`. **Result:** 16 cells survive filtering — small but enough to confirm the pipeline.
- Updated `projects/orientation_tuning/config.yaml:2` to point at `data/...`.
- README told me to set `GEMINI_API_KEY`, but `src/llm/llm_calling.py:114` reads `GOOGLE_API_KEY`. Added both to `.env`.
- Ran `python -m src.cli run projects/orientation_tuning/config.yaml --evolution.n_generations=1 --evolution.n_islands=2 --evolution.batch_size=2 --evolution.topology="[1,0]" --scoring.timeout_s=60 --scoring.gradient_descent.max_iter=50`.
  - Data loaded, JAX gradient descent dropped init loss 211 → final 131 on seed models.
  - Gemini calls reached Google (429 = free-tier 5 req/min on `gemini-2.5-flash`).
  - **Pipeline is fully wired.** `population.jsonl` has 6 program entries.

### Overview doc
- Wrote `overview.md` mapping: CLI → Config → TaskSpec → evolution loop, the Program dataclass, scoring sandboxing, LLM layer, data contract, output layout, gotchas, and "next questions" worth answering.

### Workflow setup (this commit)
- `CLAUDE.md` — working conventions (env, run command, .env keys, journal cadence, before-non-trivial-changes proposal rule, verify-before-claiming-done).
- `journal/2026-05-23.md` — this file.
- `.gitignore` cleaned up: removed a stray `git rm --cached -r program_databases` line that was being treated as a pattern; removed `CLAUDE.md` from ignore list (we want to commit it); added `data/*.npy *.npz *.mat *.h5 *.hdf5`.

### Anthropic provider support
- Decided **provider is inferred from model-name prefix**: `gemini-*` → `GoogleModel`, `claude-*` → `AnthropicModel`. Model string is the single source of truth; no separate `provider:` config field. Per-role/mixed lists work for free since `LLMsConfig` already accepts `ValidLLMs | list[ValidLLMs]`.
- `src/llm/llm_calling.py`: extracted `_build_model(model_name) -> Model` that dispatches by prefix and reads the matching env var (`GOOGLE_API_KEY` or `ANTHROPIC_API_KEY`). Unknown prefix → `UserError`. `call_llm()` no longer hard-requires `GOOGLE_API_KEY`.
- `src/io/config.py`: extended `ValidLLMs` with `claude-haiku-4-5`, `claude-sonnet-4-5`, `claude-sonnet-4-6`, `claude-opus-4-5`, `claude-opus-4-6`, `claude-opus-4-7`. These match `pydantic_ai.models.anthropic.LatestAnthropicModelNames`.
- `requirements.txt`: **no changes needed**. `anthropic` and `pydantic-ai>=1.0` were already pulling in `pydantic_ai.models.anthropic` and `pydantic_ai.providers.anthropic`. Verified by importing both.
- **Live ping test**: added `test_call_llm_provider_ping(model_name, env_var)` to `tests/llm/test_llm_calling.py`, parametrized over both providers, marked `@pytest.mark.live`, skips per-provider if its key isn't set. Real 5-token "reply with 12345" call — catches auth, model-name, and network-path failures in one shot. Chose to extend the existing test file rather than make a new one since `@pytest.mark.live` was already established there.
- **Verified both providers end-to-end** via `asyncio.run(call_llm(...))`: `gemini-2.5-flash-lite` → `'12345'`, `claude-haiku-4-5` → `'12345'`. `ANTHROPIC_API_KEY` added to `.env`.

### Test infra: pytest-asyncio + pytest.ini
- Installed `pytest-asyncio==1.3.0` in the `edgar` env and pinned it in `requirements.txt`. Without it, every async test fails at collection (`async def functions are not natively supported`). Predated today's work but was blocking three different invocations.
- Created `pytest.ini` at repo root with `asyncio_mode = auto` (no per-test `@pytest.mark.asyncio` decorator needed; the existing ones stay as no-op documentation) and registered the `live` marker to kill the `PytestUnknownMarkWarning` noise that was hiding real failures earlier today.
- Chose `pytest.ini` over `pyproject.toml` to avoid implying a packaged install — repo deliberately has no `pyproject.toml`/`setup.py` per CLAUDE.md.
- **Verified**: `pytest tests/llm/test_llm_calling.py -m "not live"` → all 3 FakeLLM tests pass, 7 live tests properly deselected. `pytest tests/llm/test_llm_calling.py::test_call_llm_provider_ping` → Anthropic case passed, Gemini case 429'd on day quota (real provider issue, not a framework issue).

### Diagnostic scripts (scripts/)
- Added `scripts/ping_llms.py`, `scripts/check_loader.py`, `scripts/smoke.sh` — single-purpose human-runnable diagnostics that mirror what the smoke pipeline exercises, so individual layers can be poked without running the full thing. Self-bootstrapping (no `PYTHONPATH=.` needed). Follow the existing `tests/system/fake_runner.py` pattern: tests assert, scripts print.

## Decisions

- **No `edgar` console script for now.** Repo has no `pyproject.toml`/`setup.py`; README's `edgar run ...` examples don't work. We use `python -m src.cli run ...`. Revisit if we want a real install.
- **Don't commit dummy data.** Gitignore `data/*.npy` etc.; commit `data/_generate_dummy.py` so anyone can regenerate.
- **One journal file per day** under `journal/YYYY-MM-DD.md`. Simpler than a rolling file.
- **Branch model:** stay on `vmsr` until we have something worth proposing back; don't push to `reilly_image_model_debug` directly.

## Open / Next session

### Immediate next step
- **Resolve the Anthropic temperature schedule incompatibility.** `src/io/task_spec.py:230` emits `temperature = 1 + exp(-gen/n_gen)` ∈ [1.37, 2.0], which Anthropic rejects (400 `temperature: range: 0..1`). Options were laid out in chat: (A) clamp at call site, (B) rescale schedule to [0, 1] universally, (C) per-provider rescale at the call boundary (recommended — preserves Gemini dynamics exactly), (D) per-provider schedule. **Decision needed** before any A/B run with Claude.
- **Do a real A/B run** on `orientation_tuning` once the temperature is sorted: same config, swap `model_llm` between `gemini-2.5-flash` and `claude-haiku-4-5`, compare population quality after a few generations. This is the whole point of getting Anthropic wired in.
- **Gemini daily quotas are exhausted for today** (both `gemini-2.5-flash` and `gemini-2.5-flash-lite` hit `GenerateRequestsPerDayPerProjectPerModel-FreeTier`). Wait ~24h or enable billing in Google AI Studio. Anthropic still has budget.

### Things to flag
- **Rotate BOTH API keys.** Both were pasted in chat — Gemini yesterday, Anthropic today. Assume both leaked. New keys in `.env`; the old ones should be revoked in Google AI Studio and console.anthropic.com.
- **Free-tier Gemini limits will keep biting.** Either enable billing on Google AI Studio or run smaller experiments. Anthropic key has more budget per Virginia — prefer Claude for any non-trivial sweep.
- **Stray stdout line `Both GOOGLE_API_KEY and GEMINI_API_KEY are set. Using GOOGLE_API_KEY.`** comes from somewhere in the Google SDK chain. Cosmetic but noisy in logs; could drop `GEMINI_API_KEY` from `.env` since nothing reads it.

### Followups not on today's list
- `requirements.txt` change should ideally be tested on Linux too before merging back to `reilly_image_model_debug`.
- `overview.md`'s "Next questions" section lists 4 followups worth doing eventually (fingerprint shape, dedup inner vs outer, family tree code, fake LLM for offline dev).
