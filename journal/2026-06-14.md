# 2026-06-14

Picked back up after a gap. Goals: figure out where we left off, check the last full run, sync with the team's repo (`upstream`), and address review feedback on our PRs.

## Aims
- [x] Confirm branch + review working conventions
- [x] Check status of the last full run (TMUX, 06-07)
- [x] Re-derive the dashboard / run commands
- [x] Check what the team did on the original repo (`reillytilbury/EDGAR` gamma)
- [x] Safeguard uncommitted local edits
- [x] Merge `upstream/gamma` into `vmsr_gamma`
- [x] Address PR #40 review (timed() decorator) and push
- [x] Add "check upstream every session" convention to CLAUDE.md

## Done

### Last full run — completed
The TMUX session `edgar` finished its run on 06-07. `program_databases/06-07/16-34-11/status.json`: `state: complete`, 12 gens, 80/80 LLM ok, 80/80 scored, 0 timeouts/inf, model `claude-sonnet-4-6` (~376k in / ~46k out tokens). No resume needed.

Dashboard (run from repo root, OR pass an absolute path — a relative path from `~` fails):
```bash
JAX_PLATFORMS=cpu edgar dashboard \
  /groups/ahrens/home/ruttenv/python_packages/EDGAR/program_databases/06-07/16-34-11 \
  --host 0.0.0.0 --no-open --port 8765
# then on the laptop: ssh -L 8765:localhost:8765 ruttenv-ws1 ; open http://localhost:8765
```
New run: `JAX_PLATFORMS=cpu edgar run projects/orientation_tuning/config.yaml`.

### Wired up `upstream` and synced with the team
`upstream` (= `reillytilbury/EDGAR`) wasn't a configured git remote (only referenced in prose). Added it. Team had advanced `gamma` by 6 commits since our fork point (rajah/rajnutakki: dashboard tweaks, STALE_THRESHOLD bump, `.DS_Store`/file cleanup, CI fork-gating, "uv run" docs, #42 merge).

Merged `upstream/gamma` into `vmsr_gamma`. Conflicts resolved:
- `dashboard/data.py` `STALE_THRESHOLD_S`: kept our **300s** (theirs was 180).
- `dashboard/latex_cache.py` `_llm_from_task_spec`: reconciled both intents — prefer `jax_model_translator_llm` (upstream's deliberate choice for LaTeX) **and** fall back to `model_llm`, with list handling (our feature). Real specs have both keys.

Verified: `edgar test-fake` (Run complete, baseline ranking unchanged) + `pytest tests/dashboard` (latex_cache 10/10 after the fix).

### Safeguarded local edits (were uncommitted, now in history)
- `3193900` dashboard: auto-load cached LaTeX (`/latex_cache`) + STALE_THRESHOLD 300s
- `59cc861` local: orientation_tuning `data_path` -> `/data` (kept separate so it never leaks into an upstream PR)
- `36baafe` stop tracking `.DS_Store` + gitignore it

### PR #40 review addressed (unblocks #41)
rajnutakki asked to replace the repeated `with stage_timer(...)` blocks in `run.py` with one-line aliases (they drafted a `timed` decorator). Implemented `timed()` in `edgar/io/metrics.py` (resolves active metrics via `get_active_metrics()`, pops `n_items`), and refactored `run.py` to module-level `t_*` aliases. Updated `test_runner_persist.py` to patch the `t_score` alias the loop now calls.
- On `vmsr_gamma`: commit `038b0cf`.
- On `pr/observability`: re-applied against that branch's simpler `run.py` (no resume/prerender), rebased onto `origin/pr/observability` (which had a new `1904f52` legend-labels commit), pushed -> PR #40 now shows commit `6ae44d4a`. Awaiting re-review.
- Verified on both branches: `pytest tests/dashboard/test_metrics.py tests/dashboard/test_runner_persist.py` all green.

### Conventions
- `8039acf` CLAUDE.md: added "check the team's repo every session" rule (fetch upstream, list PRs, check our own PRs for requested changes) + refreshed PR status. README.md: remote-server tmux/port-forward run guide.

## Commit range (vmsr_gamma, this session)
`3193900..8039acf` (own commits: `3193900`, `59cc861`, `36baafe`, merge `39600d0`, `038b0cf`, `8039acf`). PR branch: `pr/observability` -> `6ae44d4a` (pushed). None of `vmsr_gamma` pushed yet (ahead of `origin/vmsr_gamma`).

## Next
- Push `vmsr_gamma` to `origin` when ready (currently ahead, not pushed).
- Watch PR #40 re-review; once merged/approved, #41 (resume) should merge.
- Consider pulling `1904f52` (dashboard legend-label merge, currently only on `pr/observability`) into `vmsr_gamma`.
- New upstream PR #43 (`doc_bot`) is open — review when convenient.
- No GPU on this box: keep prefixing real runs with `JAX_PLATFORMS=cpu`.
