# Environment variables
.env
.vscode/ 
.DS_Store

CLAUDE.md
.codex

# output folders to ignore
git rm --cached -r program_databases
program_databases/
figures/gradopt_tests/
test_output/
sample_plots/
journal/ 

# Python cache
__pycache__/
*.pyc
*.pyo
*.pyd

# Editable-install metadata (`pip install -e .`)
*.egg-info/

*scratchpad.ipynb
*.png

# Cluster run logs (per-project, regenerated each submission)
cluster_logs/

# Local symlinks / scratch artifacts (keep runs reproducible in task_spec git_dirty)
data
test_image.png